git fetch && git merge origin main == git pull

git pull [<remote>/<branch>]
git pull origin main


# fetch (possible if you have changes)
git fetch
git fetch origin/primeagen
git log origin/primeagen
git merge origin/primeagen
