# manages ssh keys
eval $(ssh-agent)

# add key to agent
ssh-add ~/.ssh/id_rsa
ssh-add ~/.ssh/id_ed25519

# list loaded keys
ssh-add -l

# list full public keys
ssh-add -L

# remove a key
ssh-add -d ~/.ssh/id_ed25519

# remove all keys
ssh-add -D

# kill ssh agent
ssh-agent -k
