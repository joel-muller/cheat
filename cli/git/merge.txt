# Merge branch into branch i am currelty in
git merge branch
git merge --no-ff <branchname>
git merge --abort

Fast forward merge, when having, following, and merging vscode into main, it just moves the pointer from vscode to main without making a acutal merge, this is called fast forward merge

      C     delete_vscode
     /
A - B       main


# Resolve conflicts with checkout
# when you know you have conflicts and want to merge, just try to merge main into the future branch. and try to resolve conflicts in the future branch with opening an editor and do it manually. Then go back to the main branch and merge the future branch. You can also stay in the main branch and merge the future branch and resolve it with checkout
git checkout --theirs path/to/file
git checkout --ours path/to/file
